<UserControl x:Class="Galery.Resources.Loading"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Galery.Resources"
             mc:Ignorable="d">
    <StackPanel Visibility="{Binding LoadingVizibility}" Orientation="Horizontal" VerticalAlignment="Center">
        <ProgressBar
                      Style="{DynamicResource MaterialDesignCircularProgressBar}"
                      HorizontalAlignment="Center"
                      Margin="10"
                      IsIndeterminate="True"
                      Value="0"/>
        <TextBlock Text="Загрузка..." FontSize="16" Foreground="#FF673AB7" VerticalAlignment="Center"/>
    </StackPanel>
</UserControl>
